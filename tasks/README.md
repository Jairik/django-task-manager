# tasks

Main Django app. Root modules: `models.py`, `urls.py`, `limits.py`, and list/toolbar UI helpers (`project_list_ui.py`, `task_list_ui.py`). Subfolders hold views, forms, queries, templates, and migrations.
